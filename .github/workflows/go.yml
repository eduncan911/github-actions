name: Go
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    
    steps:

    - name: Set up
      uses: actions/setup-go@v1
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v1

    - name: Get dependencies
      run: |
        go version
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi
        go mod init .
        go get -v github.com/mattn/goveralls@v0.0.2
        #GOBIN=$(pwd) go install -v github.com/mattn/goveralls

    - name: Build
      run: go build -v ./...
    
    - name: Test
      run: |
        go test -covermode count -coverprofile cover.out
        goveralls -v -service github-actions -repotoken ${{ secrets.coveralls }} -coverprofile cover.out
      
    - name: Benchmark
      run: go test -test.run Benchmark -cpu 1 -bench .
